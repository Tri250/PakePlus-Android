# 签名配置示例文件
# 复制此文件为 keystore.properties 并填入实际值
# 注意：keystore.properties 不应提交到版本控制

storeFile=release-keystore.jks
storePassword=your_store_password
keyAlias=your_key_alias
keyPassword=your_key_password
